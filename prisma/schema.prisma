generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

enum STATUS{
  SENT
  CONFIRMED
  REJECTED
}

model Users{
  userId String @id @default(uuid())
  username String @unique
  name String
  email String @unique
  password String
  
  userFriends Friends[] @relation("UserFriends")
  friendRelations Friends[] @relation("FriendRelation")
  userStreak Streak? @relation("userStreak")
}
model Friends{
  userId String
  friendId String
  status STATUS @default(SENT)
  user Users @relation("UserFriends", fields: [userId], references: [userId])
  friend Users @relation("FriendRelation", fields: [friendId], references: [userId])

  @@id([userId,friendId])
}
model Streak{
  userId String @unique
  streak_count Int @default(0)
  last_updated DateTime @default(now())
  user Users @relation("userStreak",fields: [userId],references: [userId])
}
model WaitingList{
  emailId String @unique
}